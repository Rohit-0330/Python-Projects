# 🔐 Random Password Generator

A Python utility tool to generate strong random passwords containing letters, numbers, and symbols.

---

## 🚀 Features
- Generates secure passwords
- Customizable length
- Uses Python’s `string` and `random`
- No external libraries required
